name: Periodically close purchased packs

on:
  schedule:
  - cron: '0 */3 * * *'

  workflow_dispatch:
      inputs:
        chainType:
          description: 'Chain type'
          required: true
          default: 'testnet'

jobs:
  close_packs:
    name: Close purchased packs
    runs-on: ubuntu-latest
    env:
      MANAGER_PASSPHRASE: ${{ secrets.MANAGER_PASSPHRASE }}
      LEDGER_TYPE: ${{ github.event.inputs.chainType }}

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.9.9

    - name: Set up Poetry
      uses: abatilo/actions-poetry@v2.1.6
      with:
          poetry-version: 1.1.6

    - name: Install python dependencies
      run: poetry install

    - uses: pre-commit/action@v3.0.0
      name: "Linters and formatters check"
      with:
        extra_args: --all-files --show-diff-on-failure

    - name: Close purchased packs
      run: |
        PYTHONPATH="." poetry run python src/cities/packs/closeout.py

    - name: Commit files
      run: |
        git config --local user.email "millionalgos@pm.me"
        git config --local user.name "github-actions[bot]"
        git commit -m "chore: executed pack closeouts 🤖" -a

    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
